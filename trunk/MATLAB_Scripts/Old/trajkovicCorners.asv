function [ outMtrx ] = trajkovicCorners( inMtrx )

outMtrx = double(zeros(size(inMtrx,1),size(inMtrx,2)));
for i = 2:size(inImage,1)-1
    for j = 2:size(inImage,2)-1 
        C  =  inMtrx(i,j);
        A  =  inMtrx(i,j+1);
        Ap = inMtrx(i,j-1);
        B  =  inMtrx(i-1,j+1);
        Bp = inMtrx(i+1,j-1);
        D  =  inMtrx(i-1,j);
        Dp =  inMtrx(i+1,j);
        E  =  inMtrx(i-1,j-1);
        Ep =  inMtrx(i+1,j+1);
        
end

